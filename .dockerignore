.git
.github
.claude
.idea
.vscode

vendor/
var/cache/*
var/log/*
node_modules/
public/assets/

# Local environment overrides and secrets must not enter the image.
.env.local
.env.*.local

# Private keys must never enter the build context: the image is deployed to the
# very server the backups are encrypted against.
*.key

Dockerfile
compose.yaml
compose.override.yaml
.dockerignore
README.md
