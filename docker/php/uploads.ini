; Raise upload limits above PHP's stock defaults (2M/8M) so user-submitted
; photos (pfp, recipe images) reach Symfony's own validation instead of being
; silently truncated/rejected by PHP before the app ever sees them.
upload_max_filesize = 10M
post_max_size = 12M
