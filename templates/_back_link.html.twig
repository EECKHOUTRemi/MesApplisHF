{# Lien de retour affiché en haut de page (remplace l'ancien fil d'Ariane).
   Surchargez `back_path` pour cibler une page précise plutôt que l'historique :
   {{ include('_back_link.html.twig', { back_path: path('app_macuisine_index') }) }} #}
<div class="mb-3">
    <a href="{{ back_path|default('javascript:history.back()') }}" class="btn btn-link text-decoration-none px-0">
        <i class="bi bi-arrow-left me-1" aria-hidden="true"></i>{{ back_label|default('Retour') }}
    </a>
</div>
