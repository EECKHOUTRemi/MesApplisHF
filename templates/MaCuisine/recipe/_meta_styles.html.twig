{# Styles des pastilles de métadonnées (_meta_pills.html.twig) et des notes en lecture seule
   (_rating_static.html.twig). À inclure dans le bloc `stylesheets` de chaque page concernée. #}
<style>
    .meta-pill {
        background: #fff5ef;
        color: #b3502f;
        border-radius: 999px;
        padding: .35rem .9rem;
        font-size: .875rem;
        display: inline-flex;
        align-items: center;
        gap: .35rem;
    }
    .rating-static i {
        color: #e0d4cd;
        font-size: .9rem;
    }
    .rating-static-difficulty i.is-on {
        color: #dc3545;
    }
    .rating-static-cost i.is-on {
        color: #efd42b;
    }
</style>
