{% extends 'base.html.twig' %}

{% block title %}Nouvelle mesure{% endblock %}

{% block body %}
<div class="container py-5">

    {{ include('_breadcrumb.html.twig', { items: [
        { label: 'Accueil', path: path('app_index') },
        { label: 'MonPoids', path: path('app_mon_poids_global') },
        { label: 'Mensurations', path: path('app_MonPoids_measurement_index') },
        { label: 'Nouvelle mesure' },
    ] }) }}

    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="d-flex align-items-center mb-4">
                <a href="{{ path('app_MonPoids_measurement_index') }}" class="btn btn-outline-secondary me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <h1 class="fw-bold mb-0"><i class="bi bi-plus-circle text-primary me-2"></i>Nouvelle mesure</h1>
            </div>

            <div class="card shadow-sm">
                <div class="card-body p-4">
                    {{ include('MonPoids/measurement/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
