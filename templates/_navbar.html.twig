<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_index') }}">
            <picture>
                <source srcset="{{ asset('icons/logo.png') | imagine_filter('logo_avif') }}">
                <source srcset="{{ asset('icons/logo.png') | imagine_filter('logo_webp') }}">
                <source srcset="{{ asset('icons/logo.png') | imagine_filter('logo_png') }}">
                <source srcset="{{ asset('icons/logo.png') | imagine_filter('logo_jpeg') }}">
                <img src="{{ asset('icons/logo.png') }}" alt="MAHF logo">
            </picture>
            <span class="fw-bold">MesApplisHF</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Ouvrir le menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if 'macuisine' in app.request.pathInfo %}active{% endif %}" href="{{ path('app_macuisine_index') }}" >MaCuisine</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if 'monpoids' in app.request.pathInfo %}active{% endif %}" href="{{ path('app_mon_poids_global') }}">MonPoids</a>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% set currentRoute = app.request.attributes.get('_route') %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <button class="nav-link d-flex align-items-center gap-2" type="button" id="adminDropdown" data-bs-toggle="dropdown" aria-expanded="false" title="Administration">
                            <i class="bi bi-shield-lock-fill fs-4" aria-hidden="true"></i>
                            <span class="d-lg-none">Admin</span>
                            <span class="visually-hidden d-none d-lg-inline">Administration</span>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="adminDropdown">
                            <li><h6 class="dropdown-header">Administration</h6></li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_user_index') }}">
                                    <i class="bi bi-people-fill"></i> Utilisateurs
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_relationship_index') }}">
                                    <i class="bi bi-people"></i> Relations
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" tabindex="-1" href="{{ path('app_admin_macuisine_recipe_index') }}">
                                    <i class="bi bi-journal-richtext"></i> Recettes
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" tabindex="-1" href="{{ path('app_macuisine_ingredient_index') }}">
                                    <i class="bi bi-basket-fill"></i> Ingrédients
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" tabindex="-1" href="{{ path('app_admin_ma_cuisine_utensil_index') }}">
                                    <i class="bi bi-tools"></i> Ustensiles
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" tabindex="-1" href="{{ path('app_admin_ma_cuisine_category_index') }}">
                                    <i class="bi bi-tag-fill"></i> Catégories
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" tabindex="-1" href="{{ path('app_admin_macuisine_images_index') }}">
                                    <i class="bi bi-images"></i> Images
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_admin_monpoids_bmi_index') }}">
                                    <i class="bi bi-speedometer2"></i> IMC
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_admin_monpoids_measurement_index') }}">
                                    <i class="bi bi-rulers"></i> Mesures
                                </a>
                            </li>
                        </ul>
                    </li>
                {% endif %}
                <li class="nav-item">
                    {# Rendue même à zéro, et masquée : le flux Mercure l'incrémente sans recharger la page. #}
                    {% set unreadCount = unread_messages_count() %}
                    <a class="nav-link d-flex align-items-center gap-2 {% if 'chat' in currentRoute %}active{% endif %}" href="{{ path('app_friends_chat_index') }}" title="Chat">
                        <i class="bi bi-chat-dots-fill fs-4" aria-hidden="true"></i>
                        <span class="d-lg-none">Chat</span>
                        <span class="visually-hidden d-none d-lg-inline">Chat</span>
                        <span id="chatUnreadBadge" class="badge rounded-pill bg-danger {{ unreadCount > 0 ? '' : 'd-none' }}" data-count="{{ unreadCount }}">
                            <span data-chat-total>{{ unreadCount }}</span>
                            <span class="visually-hidden">message(s) non lu(s)</span>
                        </span>
                    </a>
                </li>
                <li class="nav-item">
                    {% set pendingCount = pending_friend_requests_count() %}
                    <a class="nav-link d-flex align-items-center gap-2 {% if currentRoute starts with 'app_friends' and currentRoute != 'app_friends_chat_index' %}active{% endif %}" href="{{ path('app_friends_index') }}" title="Amis">
                        <i class="bi bi-people-fill fs-4" aria-hidden="true"></i>
                        <span class="d-lg-none">Amis</span>
                        <span class="visually-hidden d-none d-lg-inline">Amis</span>
                        {% if pendingCount > 0 %}
                            <span class="badge rounded-pill bg-danger" title="{{ pendingCount }} demande(s) en attente">
                                {{ pendingCount }}
                                <span class="visually-hidden">demande(s) d'ami en attente</span>
                            </span>
                        {% endif %}
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <button class="nav-link d-flex align-items-center gap-2 mt-1" type="button" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false" title="Profil">
                        {% set imgPath = app.user and app.user.image ? 'uploads/pfp/' ~ app.user.image : null %}
                        {% if imgPath and imgPath is ondisk %}
                            <picture>
                                <source srcset="{{ asset(imgPath) | imagine_filter('pfp_navbar_avif') }}" type="image/avif">
                                <source srcset="{{ asset(imgPath) | imagine_filter('pfp_navbar_webp') }}" type="image/webp">
                                <source srcset="{{ asset(imgPath) | imagine_filter('pfp_navbar_png') }}" type="image/png">
                                <source srcset="{{ asset(imgPath) | imagine_filter('pfp_navbar_jpeg') }}" type="image/jpeg">
                                <img src="{{ asset(imgPath) }}" alt="Profil" loading="lazy" class="rounded-circle" width="32" height="32" style="object-fit: cover;">
                            </picture>
                        {% else %}
                            <i class="bi bi-person-circle fs-4" aria-hidden="true"></i>
                        {% endif %}
                        <span class="d-lg-none">Profil</span>
                        <span class="visually-hidden d-none d-lg-inline">Profil</span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdown">
                        <li>
                            <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_profil_index') }}">
                                <i class="bi bi-person-circle" aria-hidden="true"></i> Profil
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_settings_index') }}">
                                <i class="bi bi-pencil-square" aria-hidden="true"></i> Modifier le profil
                            </a>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item d-flex align-items-center gap-2 text-danger" href="{{ path('app_logout') }}">
                                <i class="bi bi-door-open-fill" aria-hidden="true"></i> Se déconnecter
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
