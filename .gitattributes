# Shell scripts are executed on Linux (deploys, cron backups): always check out
# with LF endings. A CRLF shebang fails with a misleading
# "bad interpreter: No such file or directory".
* text=auto eol=lf
*.sh    text eol=lf
*.php   text eol=lf
bin/*   text eol=lf
*.bat   text eol=crlf
